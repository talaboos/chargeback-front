services:
  app:
    image: chargeback-front
    container_name: chargeback-front
    build:
      context: ./
      dockerfile: Dockerfile
      args:
        - ENV=${NODE_ENV:-}
    restart: unless-stopped
    ports:
      - "${NODE_PORT:-3000}:3000"
